version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: complif 
      POSTGRES_PASSWORD: complif 
      POSTGRES_DB: onboarding_db
    ports:
      - '5400:5432'
